name: Hello world action
on: push
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - run: ' sed -i "s/KEY/${{secrets.GMAPAPI}}/g" ".github/workflows/testkey"'
        shell: bash
        env:
          GMAP_KEY: ${{secrets.GMAPAPI}}
