name: Hello world action
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          test: ${{ secrets.GMAPS_API_KEY }}
        run: |
              echo "$test"
