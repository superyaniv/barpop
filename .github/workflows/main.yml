name: Hello world action
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          GMAPSECRET: ${{ secrets.GMAPS_API_KEY }}
        run: |
              echo "$GMAPSECRET"
