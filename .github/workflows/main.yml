name: Hello world action
on: push
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          GMAPS_KEY: ${{ secrets.GMAPS_API_KEY }}
        run: |
              echo "$GMAPS_KEY"
